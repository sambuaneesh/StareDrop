use anyhow::{Result, bail};

pub fn encrypt_stub(_data: &[u8], _password: &str) -> Result<Vec<u8>> {
    bail!("encryption is planned for Phase 5")
}

pub fn decrypt_stub(_data: &[u8], _password: &str) -> Result<Vec<u8>> {
    bail!("encryption is planned for Phase 5")
}
